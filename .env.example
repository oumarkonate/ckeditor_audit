# Copy this file to .env and fill in the values for your project.
# .env is not versioned — each user maintains their own.

# Required — absolute path to the project being audited
CKEDITOR_AUDIT_PROJECT_ROOT=/path/to/your/project

# Optional — additional globs (comma-separated) to search for plugin usages
# beyond the default CKEDITOR_AUDIT_CONFIGS_GLOB.
# Useful for entry files, YAML configs, PHP constants, JS plugin registries, etc.
# CKEDITOR_AUDIT_EXTRA_GLOBS=

# Optional overrides for CKEditor-specific audit tools (defaults shown)
# CKEDITOR_AUDIT_PLUGINS_GLOB=assets/ckeditor/ckeditor5-*
# CKEDITOR_AUDIT_CONFIGS_GLOB=assets/config/ckeditor/*_config.js

# Version labels used in audit reports — set these to match your migration.
# Example: migrating from CKEditor 5 v26 to v47
# CKEDITOR_AUDIT_LEGACY_LABEL=v26
# CKEDITOR_AUDIT_TARGET_LABEL=v47

# Optional overrides for file search (defaults shown)
# Comma-separated directory names to skip during file search
# CKEDITOR_AUDIT_EXCLUDE_DIRS=node_modules,.git,dist,build,vendor,coverage,.nyc_output,__pycache__,.next,.cache

# File extensions searched by grep_code, find_class, ast_search, etc.
# CKEDITOR_AUDIT_EXTENSIONS=js,ts,jsx,tsx,yaml,yml,php,scss,css

# Maximum number of results returned by grep/find tools (1-500)
# CKEDITOR_AUDIT_MAX_RESULTS=50

# Search backend: "auto" (default), "rg" (ripgrep), or "python"
# "auto" uses ripgrep if the rg binary is found on PATH, falls back to Python
# CKEDITOR_AUDIT_BACKEND=auto

# Whether to respect .gitignore when searching (true/false)
# CKEDITOR_AUDIT_RESPECT_GITIGNORE=true

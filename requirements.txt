# MCP Python SDK — installed from git main (same source as 01_terminal_server)
# MCPServer lives in this branch and is not yet published on PyPI
mcp @ git+https://github.com/modelcontextprotocol/python-sdk.git@main

# Pydantic v2 — used for structured tool outputs (auto-generates JSON schemas for Claude)
pydantic>=2.0

# Loads project-specific env vars from a .env file at server startup.
python-dotenv>=1.0

# Fast fuzzy matching for symbol and file name lookups
# Gracefully degraded if absent (fuzzy search returns empty list)
rapidfuzz>=3.0

# AST structural search for PHP, JS, TS, TSX via tree-sitter
# Gracefully degraded if absent (falls back to regex patterns)
ast-grep-py>=0.38

# pathspec — used by ripgrep backend for .gitignore integration
pathspec>=0.12
